# Copy this file to .env and adjust values as needed.
# .env is git-ignored — never commit real secrets.
#
# No external API keys are required yet. As pipeline stages start
# integrating real services, their required variables will be added
# here (documented in CLAUDE.md) before any code depends on them.

LOG_LEVEL=INFO
