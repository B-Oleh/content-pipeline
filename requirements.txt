python-dotenv>=1.0.0
pytest>=8.0.0

# Production pipeline (Script Agent / Asset Acquisition / Voice / Telegram delivery).
# requests: Pexels/Pixabay JSON APIs and Telegram Bot API (incl. multipart video upload) --
#   the standard library alternative would mean hand-rolling multipart/form-data encoding.
# google-genai: official current Gemini SDK (see scripts/production/providers/llm.py).
# edge-tts: free Microsoft Edge text-to-speech, used for narration + word-level timing.
# Pillow: real text layout (measurement, wrapping, font-size fitting) for info_card.py's title/
#   body overlay text, composited via ffmpeg's `overlay` filter -- ffmpeg's own `drawtext` filter
#   has no word-wrap or auto-fit, which is what caused text to run off the frame (see that
#   module's docstring). >=10.1.0 for ImageFont.load_default(size=...)'s scalable fallback font.
requests>=2.31.0
google-genai>=1.0.0
edge-tts>=6.1.0
Pillow>=10.1.0
